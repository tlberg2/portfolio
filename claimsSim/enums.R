# enum / type defs for user input options

# TODO: not using this rn -> do we even want/need it ??

# Supported Frequency distributions
FrequencyDist <- list(
  poisson = "poisson",
  binomial = "binomial",
  negative_binomial = "negative_binomial"
)

# Supported severity distributions
SeverityDist <- list(
  gamma = "gamma",
  lognormal = "lognormal"
)
